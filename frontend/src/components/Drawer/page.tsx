import {
  Drawer,
  IconButton,
  List,
  ListItem,
  ListItemButton,
  ListItemIcon,
  ListItemText,
} from "@mui/material";
import * as React from "react";
import Menu from "@mui/icons-material/Menu";

interface DrawerProps {
  open: boolean;
}



export default function DrawerComponent({ open }: DrawerProps) {
  return (
    <>
      
    </>
  );
}
